import { TextoUpperCasePipe } from './texto-upper-case.pipe';

describe('TextoUpperCasePipe', () => {
  it('create an instance', () => {
    const pipe = new TextoUpperCasePipe();
    expect(pipe).toBeTruthy();
  });
});
